# Copy this file to .env before starting Docker Compose.
# Use local-only values and never commit the populated .env file.

MONGODB_USERNAME=local-admin
MONGODB_PASSWORD=replace-with-a-local-password
